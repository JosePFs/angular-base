import { ToolbarTitleDirective } from './toolbar-title.directive';

describe('ToolbarTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolbarTitleDirective();
    expect(directive).toBeTruthy();
  });
});
