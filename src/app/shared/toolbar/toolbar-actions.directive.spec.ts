import { ToolbarActionsDirective } from './toolbar-actions.directive';

describe('ToolbarActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolbarActionsDirective();
    expect(directive).toBeTruthy();
  });
});
