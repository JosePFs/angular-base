import { SidenavPanelDirective } from './sidenav-panel.directive';

describe('SidenavPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavPanelDirective();
    expect(directive).toBeTruthy();
  });
});
